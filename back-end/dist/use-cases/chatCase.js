"use strict";
var chatCase;
(function (chatCase) {
    function getGroupsByUser(user) {
        return new Promise(resolve => {
        });
    }
    chatCase.getGroupsByUser = getGroupsByUser;
    function createGroups(group) {
        return new Promise(resolve => {
        });
    }
    chatCase.createGroups = createGroups;
    function removeUserGroup(user, group) {
        return new Promise(resolve => {
        });
    }
    chatCase.removeUserGroup = removeUserGroup;
    function addUserInGroup(user, group) {
        return new Promise(resolve => {
        });
    }
    chatCase.addUserInGroup = addUserInGroup;
    function renameGroup(user, group) {
        return new Promise(resolve => {
        });
    }
    chatCase.renameGroup = renameGroup;
    function checkUserIsAdmin(user, group) {
        return new Promise(resolve => {
        });
    }
    chatCase.checkUserIsAdmin = checkUserIsAdmin;
})(chatCase = exports.chatCase || (exports.chatCase = {}));
